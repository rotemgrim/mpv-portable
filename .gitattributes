* text=auto eol=lf

# Heavy binaries used to live in Git LFS. They are now distributed as a
# single .7z release asset (see bootstrap.ps1 / tools/build-payload.ps1)
# and are gitignored, so no LFS filters are needed here anymore.
*.exe binary
*.dll binary
*.pyd binary
*.onnx binary
*.zip binary
*.otf binary
*.ttf binary
